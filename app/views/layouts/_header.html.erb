<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
     <!-- Logo -->
     <a class="navbar-brand" href="/">
      <%=image_tag("icons/logo-light.png", size: "25x25", alt: "#{app_name}")  %> 
      </a>
      <a class="navbar-brand" href="/">
        Coordinate
      </a>
    </div>
     <!-- Right  block-->
    <ul class="nav navbar-nav navbar-right">
        <% unless current_page?(root_path) %>
            <li>
                <%=link_to("Home",root_path)%>   
            </li>
        <% end %>
        <% if signed_in? %>
          <li>
            <%=link_to("Users",users_path)%>
          </li>
          <li id="fat-menu" class="dropdown">
            <a href='#' class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
              <%=avatar_for(current_user, size: "25x25", alt: "#{current_user.login}", class:"img-circle")  %>
            </a>
            <ul class="dropdown-menu">
              <li> <%= link_to("Profile", current_user)%></li>
              <li><%= link_to("Settings",edit_user_path(current_user) ) %></li>
              <li class="divider"></li>
              <li><%= link_to("Help",faq_path)%></li>
              <% unless current_user.verificated? %>
                <li class="divider"></li>
                <li><%= link_to("Verification",verification_user_path(current_user))%></li>
              <%end%>
              <li class="divider"></li>
              <li> <%= link_to("Sign Out", signout_path, method: "delete") %></li>
            </ul>
          </li>
          <%else%>
            <%= render 'shared/modals/form', form: 'shared/sign_tabs', id:'sign_modal', title: 'Authorization' %>
         <li>
            <a onclick="sign_modal_call()" href="#">Authorization</a>
         </li>
           <%end%>
        </li>
        </ul>
     <!-- Right  block-->
  </div>
</nav>
