<% provide(:title,@user.login)%>
<div class="row">
  <aside class ="col-md-4">
    <section>
      <%= render 'shared/user_info', user:@user %>
    </section> 
    <section>
    <%= "Hello, #{@user.login}" if signed_in? and !current_user?(@user) %>
    </section>
  </aside>
  <div class="col-md-8">
   </div>
</div>
